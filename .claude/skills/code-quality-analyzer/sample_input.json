{"action": "analyze", "language": "python", "code_path": "/path/to/python/project", "options": {"check_security": true, "check_complexity": true, "check_tests": true, "check_best_practices": true, "min_coverage_threshold": 80, "complexity_threshold": 10, "output_format": "json"}, "sample_files": [{"file": "services/user_service.py", "content": "import sqlite3\nimport random\n\ndef get_user(user_id):\n    # Security issue: SQL injection vulnerability\n    conn = sqlite3.connect('users.db')\n    cursor = conn.cursor()\n    # Bad: Direct string concatenation\n    query = f\"SELECT * FROM users WHERE id = {user_id}\"\n    cursor.execute(query)\n    user = cursor.fetchone()\n    conn.close()\n    return user\n\ndef create_user(name, email, password):\n    # Security issue: Hardcoded secret\n    api_key = \"sk-1234567890abcdef\"\n    conn = sqlite3.connect('users.db')\n    cursor = conn.cursor()\n    cursor.execute(\"INSERT INTO users (name, email, password) VALUES (?, ?, ?)\", (name, email, password))\n    conn.commit()\n    conn.close()\n\ndef process_data(items):\n    # Complexity issue: High cyclomatic complexity\n    results = []\n    for item in items:\n        if item['type'] == 'A':\n            if item['status'] == 'active':\n                if item['category'] == 'premium':\n                    if item['level'] > 5:\n                        if item['score'] > 80:\n                            results.append(process_item_a(item))\n        elif item['type'] == 'B':\n            if item['status'] == 'active':\n                results.append(process_item_b(item))\n        elif item['type'] == 'C':\n            if item['status'] == 'pending':\n                if item['priority'] == 'high':\n                    results.append(process_item_c(item))\n    return results\n"}, {"file": "utils/helper.py", "content": "import random\n\ndef generate_token():\n    # Security issue: Not cryptographically secure\n    return ''.join(random.random() for _ in range(32))\n\ndef unsafe_eval(expression):\n    # Security issue: Code injection vulnerability\n    result = eval(expression)\n    return result\n\ndef print_debug(data):\n    # Best practice issue: Using print instead of logging\n    print(data)\n"}, {"file": "api/handler.py", "content": "def handle_request(data):\n    try:\n        result = process(data)\n        return result\n    except:\n        # Best practice issue: Bare except clause\n        return None\n"}]}